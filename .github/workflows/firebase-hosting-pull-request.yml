# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# This workflow is intended for continous deployment of the frontend to Firebase Hosting.
# CD was not setup for the project, thus the workflow has been commented out for now, so that it can be implemented at a later date


# name: Deploy to Firebase Hosting on PR
# 'on': pull_request
# jobs:
#   build_and_preview:
#     if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - run: npm ci && npm run build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOSMA_2A401 }}'
#           projectId: dosma-2a401
