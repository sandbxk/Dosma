/**
 * An enum that represents the different states of the checkbox.
 */
export enum Status {
  Unchecked = 0, // The checkbox is unchecked and displays an empty box.
  Done = 1, // The checkbox is checked.
  Skipped = 2, // The checkbox is unchecked and displays a red 'X'.
}
