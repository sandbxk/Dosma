/**
 * An enum that represents the different states of the checkbox.
 */
export enum Status {
  Done, // The checkbox is checked.
  Skipped, // The checkbox is unchecked and displays a red 'X'.
  Unchecked, // The checkbox is unchecked and displays an empty box.
}
